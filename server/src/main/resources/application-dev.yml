spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        detector:
          enabled: true

logging:
  level:
    org.hibernate.orm.jdbc.bind: trace

api:
  url: http://dev.ahmadda.com:8080

jwt:
  secret-key: ${jwt.local.secret-key}
  access-expiration: ${jwt.local.access-expiration}
