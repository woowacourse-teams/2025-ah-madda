name: Deploy Storybook

on:
  pull_request:
    types: [closed]
    paths:
      - 'client/**/*.stories.tsx'
      - 'client/**/*.stories.ts'
      - 'client/**/*.mdx'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy-storybook:
    if: github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'fe/dev'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js and pnpm
        uses: ./.github/actions/pnpm-setup-node

      - name: Install dependencies
        working-directory: ./client
        run: pnpm install --frozen-lockfile

      - name: Deploy with Chromatic
        id: chromatic
        uses: chromaui/action@latest
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          workingDir: ./client
          buildScriptName: build-storybook
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Slack deployment notification
        uses: 8398a7/action-slack@v3
        with:
          status: success
          channel: '#fe_storybook'
          text: |
            🚀 "${{ github.event.pull_request.title }}" 의 변경사항이 Storybook에 배포되었습니다!

            👉 ${{ steps.chromatic.outputs.storybookUrl }}
        env:
          SLACK_WEBHOOK_URL_STORYBOOK: ${{ secrets.SLACK_WEBHOOK_URL_STORYBOOK }}
